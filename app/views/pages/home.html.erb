<div class="home-callout text-center container-fluid">
  <h1>Welcome to DroneHub</h1>
  <h3>A membership site where drone enthusiasts and developers come to meet.</h3>
</div>
<div class="container home-signup-links">
  <div class="row">
    <% if user_signed_in? %>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Your Profile</h2>
          <% if current_user.profile %>
            <%= link_to "Edit your profile", "#", class: 'btn btn-default btn-lg btn-block' %>
            <%= link_to "View your profile", user_path(current_user.id), class: 'btn btn-default btn-lg btn-block' %>
          <% else %>
            <p class="text-center">Create your profile so that you can share your information with the community.</p>
            <%= link_to "Create your profile", user_profile_path(user_id: current_user.id), class: 'btn btn-default btn-lg btn-block' %>
          <% end %>
        </div>
      </div>
      
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Community</h2>
          <p class="text-center">Visit the community to meet developers, entrepreaneters and invesors.</p>
          <%= link_to "Visit the community", "#", class: 'btn btn-default btn-lg btn-block' %>
        </div>
      </div>
      
    <% else %>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Basic Membership</h2>
          <h4 class="text-center">Sign up for free and get basic access to the community.</h4>
          <br>
          <%= link_to "Sign up with Basic Plan", new_user_registration_path(plan: @basic_plan), class: 'btn btn-primary btn-lg btn-block'%>
        </div>
      </div>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Pro Membership</h2>
          <h4 class="text-center">Sign up for the Pro account for $10/month and
          get access to the community and contact information.</h4>
          <%= link_to "Sign up with Pro Plan", new_user_registration_path(plan: @pro_plan), class: 'btn btn-success btn-lg btn-block'%>
        </div>
      </div>
    <% end %>
  </div>
</div>